name: Ping Bot
on:
  schedule:
    - cron: "*/30 * * * *"  # 30分ごとに実行

jobs:
  ping-bot:
    runs-on: ubuntu-latest
    environment:
      name: discord-bot
    steps:
      - name: Send GET request
        run: curl -X GET ${{ vars.DISCORD_BOT_URL }}
